app.controller('beaconController', function ($scope) {
   
    angular.extend($scope, {
        home: {
            autoDiscover: true,
            zoom: 12
        }
    });
    
    // console.log(center.lat);

// center.lat, center,lng

});